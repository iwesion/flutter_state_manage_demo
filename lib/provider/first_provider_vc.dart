import 'package:flutter/material.dart';

class FirstProviderVC extends StatelessWidget {
  const FirstProviderVC({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text('Flutter and provider'),
        ),
        body: Container());
  }
}
